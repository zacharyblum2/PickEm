export type Colors = {
    mainBackground: string;
    reverseBackground: string;
    primary: string;
    primaryDark: string;
    primaryLight: string;
    textPrimary: string;
    accent: string;
    success: string;
    warning: string;
    white: string;
    black: string;
};

export type appTheme = {
    colors: Colors;
    // Add other properties if needed
};