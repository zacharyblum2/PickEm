import React from "react";
import { Text, View } from "react-native";

export const EventCard = React.memo(() => {

    // Receive data item from API
    // Render data item similar to ESPN app
    // Player 1 (rank) (record)
    // Player 2 (rank) (record) (odds)
    // 

    return (
        <View>
            <Text>Event Card</Text>
        </View>
    )
})

export default EventCard;